# JobsDB 投递控制台 - 前端

React + Vite + Tailwind CSS

## 运行

需先安装 [Node.js](https://nodejs.org/)（推荐 LTS 版），然后：

```bash
cd web
npm install
npm run dev
```

浏览器访问：<http://localhost:5173>

##  mock 说明

- 登录/注册：仅保存到 localStorage，无后端校验
- 统计、职位列表、日志、外部投递：均为 mock 数据
- 接入后端 API 后需替换为真实请求
